name: Deploy Lambda Function Code

on:
  push:
    paths:
      - "lambda-function/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  LAMBDA_BUCKET: ${{ secrets.AWS_LAMBDA_CODE_BUCKET }}
  LAMBDA_FUNCTION_NAME: ${{ secrets.AWS_LAMBDA_FUNCTION_NAME }}
  LAMBDA_ALIAS: prod
  
jobs:
  deploy-lambda:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_TERRAFORM_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Detect UTF-8 BOM in schema.json
        id: bom_check
        run: |
          if grep -q $'\xef\xbb\xbf' lambda-function/schema.json; then
            echo "bom=true" >> $GITHUB_OUTPUT
            echo "⚠️ UTF-8 BOM detected in schema.json"
          else
            echo "bom=false" >> $GITHUB_OUTPUT
            echo "✅ schema.json encoding is clean"
          fi

      - name: Fix UTF-8 BOM in schema.json
        if: steps.bom_check.outputs.bom == 'true'
        run: |
          echo "🔧 Removing UTF-8 BOM from schema.json"
          sed -i '1s/^\xEF\xBB\xBF//' lambda-function/schema.json

          echo "🔍 Verifying schema.json after fix"
          jq empty lambda-function/schema.json

      - name: Assert schema.json is BOM-free
        run: |
          if grep -q $'\xef\xbb\xbf' lambda-function/schema.json; then
            echo "❌ BOM still present after attempted fix"
            exit 1
          fi
      
      - name: Capture current Lambda alias version
        id: current
        run: |
          set -e
          VERSION=$(aws lambda get-alias \
            --function-name "$LAMBDA_FUNCTION_NAME" \
            --name "$LAMBDA_ALIAS" \
            --query FunctionVersion \
            --output text)
          echo "current_version=$VERSION" >> $GITHUB_OUTPUT
          echo "Current alias version: $VERSION"

      - name: Build Lambda ZIP
        run: |
          set -e
          cd lambda-function
          rm -f function.zip
          zip -X function.zip main.py schema.json
          unzip -l function.zip

      - name: Upload ZIP to S3
        run: |
          aws s3 cp lambda-function/function.zip \
            s3://${LAMBDA_BUCKET}/lambda/function.zip \
            --force

      - name: Deploy new Lambda version
        id: deploy
        run: |
          set -e
          aws lambda update-function-code \
            --function-name "$LAMBDA_FUNCTION_NAME" \
            --s3-bucket "$LAMBDA_BUCKET" \
            --s3-key "lambda/function.zip"

          NEW_VERSION=$(aws lambda publish-version \
            --function-name "$LAMBDA_FUNCTION_NAME" \
            --query Version \
            --output text)

          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "Published new version: $NEW_VERSION"

      - name: Update alias to new version
        run: |
          set -e
          aws lambda update-alias \
            --function-name "$LAMBDA_FUNCTION_NAME" \
            --name "$LAMBDA_ALIAS" \
            --function-version "${{ steps.deploy.outputs.new_version }}"

      - name: Rollback alias on failure
        if: failure()
        run: |
          echo "⚠️ Deployment failed. Rolling back alias..."
          aws lambda update-alias \
            --function-name "$LAMBDA_FUNCTION_NAME" \
            --name "$LAMBDA_ALIAS" \
            --function-version "${{ steps.current.outputs.current_version }}"

